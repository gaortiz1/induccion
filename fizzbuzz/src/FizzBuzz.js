import {FizzBuzzCalculator} from '../../src/FizzBuzzCalculator';

export class FizzBuzz {

    play() {
        var result = []

        for (var i = 1; i <= n; i++) {
            //result.push(getResult(i));
        }

        return result;
    }

}